{{#each places}}
<div class="card card-body mt-2 mb-2">
    <h4>{{title}}</h4>
    <p>{{details}}</p>
    <p>{{user}}</p>



</div>

{{else}}
<div class="jumbotron">
    <p class="text-center">No places are added</p>
</div>

{{/each}}